# =================================================================
# ARCHIVO: /.env (Raíz del proyecto - Configuración Principal)
# =================================================================
# Ubicación: /curiosmaze/.env

# =================================================================
# 🌐 CONFIGURACIÓN DE SERVICIOS PRINCIPALES
# =================================================================

# 🏠 Configuración del Host Principal (tu máquina)
HOST_MACHINE_IP=192.168.1.2  # ← Tu IP actual

# 🔧 Judge0 - Sistema de Ejecución de Código
JUDGE0_HOST=192.168.1.80        # ← IP actual de Judge0
JUDGE0_PORT=2358
JUDGE0_API_URL=http://192.168.1.80:2358

# 🖥️ Backend Django
BACKEND_HOST=localhost          # En desarrollo siempre localhost
BACKEND_PORT=8000
BACKEND_URL=http://localhost:8000

# 🎨 Frontend Vue.js
FRONTEND_HOST=localhost         # En desarrollo siempre localhost  
FRONTEND_PORT=5173
FRONTEND_URL=http://localhost:5173

# 📊 Base de Datos PostgreSQL
DATABASE_HOST=localhost         # Docker se encarga de esto
DATABASE_PORT=5432
DATABASE_NAME=db_curiosmaze        
DATABASE_USER=curiosmaze
DATABASE_PASSWORD=pass_curiosmaze

# 🚀 Redis Cache (Opcional)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=redis_curiosmaze_2024

# =================================================================
# 🔒 CONFIGURACIÓN DE SEGURIDAD
# =================================================================
SECRET_KEY=django-curiosmaze-super-secret-key-change-in-production-2024
DEBUG=true

# Hosts permitidos (agregar IPs según necesites)
ALLOWED_HOSTS=localhost,127.0.0.1,192.168.1.2,backend,curiosmaze-backend

# =================================================================
# 🌍 CONFIGURACIÓN DE CORS Y CONEXIONES
# =================================================================

# Orígenes permitidos para CORS (frontend -> backend)
BACKEND_CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Orígenes confiables para CSRF
CSRF_TRUSTED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# =================================================================
# ⚙️ CONFIGURACIÓN DE JUDGE0 (Límites de Ejecución)
# =================================================================
CPU_TIME_LIMIT=2.0
CPU_EXTRA_TIME=0.5
WALL_TIME_LIMIT=5.0
MEMORY_LIMIT=128000
JUDGE0_MAX_WORKERS=5
JUDGE0_TIMEOUT=30

# =================================================================
# 🐳 CONFIGURACIÓN ESPECÍFICA PARA DOCKER
# =================================================================
DOCKER_ENVIRONMENT=true
COMPOSE_PROJECT_NAME=curiosmaze